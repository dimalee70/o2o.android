package photograd.kz.photograd.models.shared


object SoundPlayer
{
    /*private var mediaPlayer: MediaPlayer? = null

    var isOn = DataHolder.sharedPref.getBoolean(Constants.soundPrefsKey, Constants.soundOn)
    @Subscribe(threadMode = ThreadMode.MAIN)
    fun onMessageEvent(event: SoundEvent)
    {
        val isPlaying = mediaPlayer?.isPlaying
        if (isPlaying == true || !isOn)
        {
            return
        }

        mediaPlayer = MediaPlayer.create(App.appComponent.getContext(), event.soundId)
        mediaPlayer?.start()
    }*/
}
